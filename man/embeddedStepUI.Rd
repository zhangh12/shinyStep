% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedded.R
\name{embeddedStepUI}
\alias{embeddedStepUI}
\title{UI for an embedded-mode step debugger}
\usage{
embeddedStepUI(
  id,
  label = id,
  height = "500px",
  theme = "textmate",
  default_body = "",
  default_fn_name = ""
)
}
\arguments{
\item{id}{Module namespace ID.}

\item{label}{Toolbar label. Defaults to \code{id}.}

\item{height}{Ace editor height as a CSS string (e.g. \code{"500px"}).}

\item{theme}{Ace editor theme name (passed to \pkg{shinyAce}).}

\item{default_body}{Initial body text pre-filled in the editor.}

\item{default_fn_name}{Initial function name pre-filled in the name input.}
}
\value{
A \code{tagList} suitable for inclusion anywhere in a Shiny UI.
}
\description{
Renders the function editor with a \strong{Debug} checkbox. Ticking Debug
makes this function a pause point the next time the host app calls
\code{\link{run_program}()}. The editor holds the function body only — do
not type \code{fn_name <- function(...) \{ \}} wrappers; they are stripped
defensively if present. The function name and argument list live in
structured inputs above the editor.
}
\details{
Pair with \code{\link{embeddedStepServer}()} using the same \code{id}.
}
